<h1><%= @task.title %></h1>

<p><%= @task.details%></p>
<p><%= @task.completed%></p>


<%= link_to "edit", edit_task_path %>

<%= link_to "delete task",
            task_path(@task),
            method: :delete,
            data: { confirm: "Are you sure?"} %>

<%= link_to "Back to the list of tasks", tasks_path %>
